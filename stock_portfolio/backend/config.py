import os

# Database configuration
DB_CONFIG = {
    "host": "localhost",
    "user": "user",
    "password": "pass",
    "database": "stock_data",
    "charset": "utf8mb4",
}

# Flask configuration
FLASK_CONFIG = {"DEBUG": True, "SECRET_KEY": "your_secret_key"}
